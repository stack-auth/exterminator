services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    command: pnpm --dir /app/agent dev
    volumes:
      - ./agent:/app/agent
      - agent_node_modules:/app/agent/node_modules
      - ./runner:/app/runner

volumes:
  agent_node_modules:
