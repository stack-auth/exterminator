services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "cd /code && pnpm dev --port 3000 --host &
             pnpm --dir /app/agent dev"
    ports:
      - "4000:4000"
      - "3000:3000"
    env_file:
      - runner/.env
    volumes:
      - ./agent:/app/agent
      - agent_node_modules:/app/agent/node_modules
      - ./runner:/app/runner

volumes:
  agent_node_modules:
